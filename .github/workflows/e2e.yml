name: End to End

on:
  pull_request:

jobs:
  end-to-end:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      NODE_ENV: test
      STRIPE_KEY: ${{ secrets.STRIPE_KEY }}
      SEED: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Build the stack
        run: docker compose -f ./cypress/docker-compose.yml up -d

      # COMEÇA A ESCUTAR TODOS OS LOGS DA API EM TEMPO REAL
      - name: Stream API logs
        run: |
          docker logs -f -t wongames_api > api.log 2>&1 &

      - name: Wait for containers stabilize
        run: sleep 15

      - name: Waiting Strapi Server
        run: |
          docker run --rm \
            --network container:wongames_api \
            jwilder/dockerize \
            -wait http://localhost:1337/admin \
            -timeout 120s \
            -wait-retry-interval 5s

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run Cypress
        run: npm run test:e2e:ci

      # MOSTRA TODOS OS LOGS CAPTURADOS DESDE O INÍCIO
      - name: Show Full API Logs
        if: always()
        run: |
          echo "================ FULL API LOGS ================"
          cat api.log

      - name: Docker Debug
        if: always()
        run: |
          echo "================ DOCKER PS ================"
          docker ps -a

          echo "================ API LAST 5000 LINES ================"
          docker logs --tail 5000 wongames_api

          echo "================ DOCKER COMPOSE LOGS ================"
          docker compose -f ./cypress/docker-compose.yml logs --timestamps

      - name: Upload Cypress Screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: cypress/screenshots
